# Requirements for EPUB to Audiobook Converter

# Core dependencies (already in main project)
# torch
# torchaudio
# librosa
# transformers
# omegaconf

# EPUB processing
ebooklib>=0.18
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Optional: Ollama integration (for text cleanup)
requests>=2.31.0

# Optional: Progress bars
tqdm>=4.65.0
